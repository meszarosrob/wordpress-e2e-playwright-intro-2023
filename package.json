{
    "scripts": {
        "format": "npx prettier ./tests ./src ./.github ./*.{ts,json,yml,md} --write",
        "test": "npx playwright test"
    },
    "prettier": {
        "singleQuote": true,
        "tabWidth": 4
    },
    "devDependencies": {
        "@playwright/test": "1.40.1",
        "@wordpress/e2e-test-utils-playwright": "0.16.0",
        "prettier": "^3.1.1"
    }
}
